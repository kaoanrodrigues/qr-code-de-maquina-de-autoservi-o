import { defineConfig } from 'vite'

// ⚠️ IMPORTANTE: Substitua 'NOME-DO-SEU-REPO' pelo nome exato do repositório no GitHub
// Exemplo: se o repo se chama 'qr-code-fast', coloque '/qr-code-fast/'
export default defineConfig({
  base: '/qr-code-de-maquina-de-autoservi-o/',
})
